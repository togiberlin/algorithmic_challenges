matrix:
  include:
    - language: node_js
      node_js:
        - "node"
      cache:
        directories:
          - "node_modules"
      before_script:
        - npm install
      script:
        - npm test

    - language: ruby
      rvm: 2.5.3
      gemfile:
        - Gemfile
      before_script:
        - bundle install
      script:
        - rspec --pattern **/spec/*_spec.rb
